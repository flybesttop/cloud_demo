# 应用端口
server:
  port: 8001
spring:
  application:
    # 应用名称
    name: consumer

# eureka 配置
eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:123456@localhost:8761/eureka

logging:
  level:
    root: INFO
